/**
 * ECharts no publica tipos para sus temas: son módulos de efecto lateral que
 * se limitan a llamar a `registerTheme`. Importarlos ya los deja disponibles
 * en `echarts.init(dom, 'dark')`.
 */
declare module 'echarts/theme/dark.js';
